{
  "name": "Financial_Record",
  "type": "object",
  "properties": {
    "surgery_id": {
      "type": "string",
      "description": "ID da cirurgia relacionada"
    },
    "patient_name": {
      "type": "string",
      "description": "Nome do paciente"
    },
    "procedure_name": {
      "type": "string",
      "description": "Nome do procedimento"
    },
    "hospital": {
      "type": "string",
      "description": "Hospital"
    },
    "surgery_date": {
      "type": "string",
      "format": "date",
      "description": "Data da cirurgia"
    },
    "invoice_amount": {
      "type": "number",
      "description": "Valor faturado"
    },
    "received_amount": {
      "type": "number",
      "description": "Valor recebido"
    },
    "payment_date": {
      "type": "string",
      "format": "date",
      "description": "Data do pagamento"
    },
    "payment_status": {
      "type": "string",
      "enum": [
        "pendente",
        "pago_parcial",
        "pago_total",
        "cancelado"
      ],
      "default": "pendente",
      "description": "Status do pagamento"
    },
    "assistant_payments": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string"
          },
          "amount": {
            "type": "number"
          },
          "paid": {
            "type": "boolean"
          },
          "payment_date": {
            "type": "string",
            "format": "date"
          }
        }
      },
      "description": "Pagamentos dos auxiliares"
    },
    "notes": {
      "type": "string",
      "description": "Observações sobre o pagamento"
    }
  },
  "required": [
    "surgery_id",
    "patient_name",
    "procedure_name"
  ]
}